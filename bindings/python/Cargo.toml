[package]
name = "rookie-cookies-python"
version = "0.5.7"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html
[lib]
name = "rookie_cookies"
crate-type = ["cdylib"]

[dependencies]
log = "0.4"
pyo3 = { version = "0.23", features = ["abi3-py37", "anyhow"] }
pyo3-log = "0.12"

[target.'cfg(windows)'.dependencies]
rookie-core = { package = "rookie-cookies", path = "../../rookie-rs", version = "0.5.7", features = ["appbound"] }

[target.'cfg(unix)'.dependencies]
rookie-core = { package = "rookie-cookies", path = "../../rookie-rs", version = "0.5.7" }
